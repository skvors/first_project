


//код условного оператора           уровень 4 лекции начиная с 4

if (условие)
    команда_1;
else
    команда_2;


//Сокращенная запись

  подробнее про ? * : *  -  проверяет если да то первый операнд, если нет то второй
  String str1 = "string";
  String str2 = "string";
  System.out.println(str1==str2 ? "the same" : "not the same");   //т. е. если да то "the same" если нет то "not the same"